# AWS-Application-Load-Balancer-with-Spot-On-Demand-EC2-Instances
This project demonstrates how to deploy both Spot and On-Demand EC2 instances behind an AWS Application Load Balancer (ALB). Each instance uses a User Data script to install Apache automatically and display whether the request is being served by a Spot Instance or an On-Demand Instance.
